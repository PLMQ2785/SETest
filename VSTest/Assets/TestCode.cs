using UnityEngine;

public class TestCode : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello, World Commit 1!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
